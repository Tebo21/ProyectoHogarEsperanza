import { FichaSocioeconomica } from './ficha-socioeconomica';

describe('FichaSocioeconomica', () => {
  it('should create an instance', () => {
    expect(new FichaSocioeconomica()).toBeTruthy();
  });
});
