import { DocumentosBeneficiarios } from './documentos-beneficiarios';

describe('DocumentosBeneficiarios', () => {
  it('should create an instance', () => {
    expect(new DocumentosBeneficiarios()).toBeTruthy();
  });
});
